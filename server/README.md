# SproutMe SMS server (PythonAnywhere mirror)

Live backend for SMS + scoring lives on PythonAnywhere at `/home/phelpsm4/sproutMe/`.

This folder mirrors the pieces touched for show-score SMS replies so changes are reviewable in git. Deploy by uploading the matching files under `services/` on PythonAnywhere and reloading `phelpsm4.pythonanywhere.com`.
